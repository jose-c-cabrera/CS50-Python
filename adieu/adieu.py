import inflect

names = []

while (True):
    try:
        name_input = input("Name: ")
        names.append(name_input)

        for item in names:
    except:
        (ValueError, IndexError):
        continue

